name: networking
description: dio 封裝:client 工廠、攔截器、統一錯誤轉換;定義 TokenProvider 契約。
publish_to: none
resolution: workspace

environment:
  sdk: ^3.7.0

dependencies:
  dio: ^5.7.0
  foundation: any

dev_dependencies:
  test: ^1.25.0
