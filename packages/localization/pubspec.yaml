name: localization
description: 多語系(官方 gen-l10n + ARB),含各 feature 文案。
publish_to: none
resolution: workspace

environment:
  sdk: ^3.12.0

flutter:
  generate: true

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: any

dev_dependencies:
  flutter_test:
    sdk: flutter
