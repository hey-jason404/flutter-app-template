name: home
description: 首頁功能:domain/data/presentation 層(項目清單與詳情頁、blocs)。
publish_to: none
resolution: workspace

environment:
  sdk: ^3.12.0

dependencies:
  bloc: ^9.0.0
  design_system: any
  flutter:
    sdk: flutter
  flutter_bloc: ^9.0.0
  foundation: any
  get_it: ^8.0.0
  go_router: ^14.6.0
  localization: any
  navigation: any
  networking: any

dev_dependencies:
  bloc_test: ^10.0.0
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.4
